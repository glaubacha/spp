# Deployment

## Vercel

Import the GitHub repo into Vercel and use the default settings for a Next.js project.

Build command:

```bash
npm run build
```

The interconnection map is static and does not require a production database.
